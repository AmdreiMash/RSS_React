import styled from 'styled-components';

const FormPageWrapper = styled.div`
  display: flex;
  flex-direction: row;
  flex: 1;
  width: 100%;
  height: 100%;
  position: relative;
`;

export default FormPageWrapper;
